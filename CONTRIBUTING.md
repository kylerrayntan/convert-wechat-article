# 参与改进

先按 README 安装依赖，修改后运行 `python3 tests/run_checks.py`。

新增公式或文档结构时，请同时补充支持范围说明、正向样本和非法结构的拒绝样本，并对照源公式检查渲染结果。保持固定依赖锁文件、只读输入、原图字节保留和失败不交付残缺 HTML 的约束。

问题反馈中请提供系统、Python/Node 版本、浏览器、发生阶段及经过脱敏的错误信息。仅使用可公开的最小复现样本；不要提交原始私密文章、账户信息、完整素材或带正文的报告。

仓库默认忽略 Word/PDF 文档；合成回归样本由测试生成。确需提交公开二进制样本时，在 Pull Request 中说明来源和再分发权限。

提交前检查 `git diff --cached`，确认不含 `node_modules`、生成文章或私人资料。
